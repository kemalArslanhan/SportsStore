@*@model IQueryable<SportsStore.Models.Product>*@
@model SportsStore.Models.ViewsModels.ProductListViewModel
<link href="/lib/bootstrap/css/bootstrap.css" rel="stylesheet" />
<h4 class="text-center text-bg-dark">Welcome to SportsStore</h4>
<!DOCTYPE html>
<html>
<head>
  
    <title></title>
</head>
<body>
    <div >
        <table class="table table-striped table-hover ">


            <tr class="bg-info table-border">
                <td>ProductID</td>
                <td>Name</td>
                <td>Description</td>
                <td>Price</td>
                <td>Category</td>
                <td></td>
            </tr>


            @foreach (var p in Model.Products)
            {

                @*<tr class="bg-light table-border" style="border: 1px solid gray;">
                    <td>@p.ProductID</td>
                    <td>@p.Name</td>
                    <td>@p.Description</td>
                    <td>@p.Price.ToString("c")</td>
                    <td>@p.Category</td>
                </tr>*@

                <partial name="PruductSummary" model="p" />


            }



        </table>
    </div>
    @{ int previousPage = Model.PagingInfo.CurrentPage - 1;
        int nextPage = Model.PagingInfo.CurrentPage + 1;

    }

    <div class="pagination">


        @*@for (int i = 1; i <= Model.PagingInfo.TotalPages; i++)
             {
                 if (i == Model.PagingInfo.CurrentPage)
                 {
                     <a asp-action="Index" asp-route-productPage="@i"
                        class="btn btn-dark disabled">@i</a>
                 }
                 else
                 {
                     <a asp-action="Index" asp-route-productPage="@i"
                        class="btn btn-outline-dark"></a>
                 }
            }*@

        @if (Model.PagingInfo.CurrentPage > 1)
        {
            <a asp-action="Index" asp-route-productPage="@previousPage" class="btn btn-outline-dark">Back</a>
        }

        @if (Model.PagingInfo.CurrentPage < Model.PagingInfo.TotalPages)
        {
            <a asp-action="Index" asp-route-productPage="@nextPage" class="btn btn-outline-dark">Next</a>
        }


    </div>
</body>

</html>


@*@foreach (var p in Model)
    {
        <div>
            <h3>@p.Name</h3>
            @p.Description
            <h4>@p.Price.ToString()</h4>
        </div>
    }*@

@*<h2></h2>
    @foreach (var p in Model.Products)
    {
        <div>
            <h3>@p.Name</h3>
            @p.Description
        <h4>@p.Price.ToString("c")</h4>
        </div>
    }


    @for (int i = 0; i <= Model.PagingInfo.TotalPages; i++)
    {
        <a asp-action="Index" asp-route-productPage="@i">Saite @i</a>
    }*@

